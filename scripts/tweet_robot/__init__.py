"""Tweet-controlled SO-101 robot package.

Modules are designed to be imported flatly (e.g. ``import config``) when the
package directory is on ``sys.path`` — matching how the sibling example scripts
in ``scripts/`` import each other. The entry point ``run_tweet_robot.py`` ensures
both this directory and ``scripts/`` are importable.
"""
